$kind: http-request
url: "{{base_url}}/v1/auth/sessions/me"
method: GET
queryParams:
  - key: email
    value: superadmin@rusik.gov.my
    disabled: true
  - key: searchTerm
    value: super
    disabled: true
body:
  type: json
  content: "{\r

    \    \"refreshToken\":\"{{refreshToken}}\"\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
settings: {}
order: 7000
